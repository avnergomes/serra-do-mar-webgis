# Overpass / Wikidata raw caches + intermediate artifacts (regenerated by the fetch_*.py scripts)
# Globbed on purpose: every collector caches to <name>_raw.json, so new ones are covered
# without editing this file (crags_raw.json was missed back when these were listed one by one).
*_raw.json
*_raw.geojson
logo_small.png

# Python
__pycache__/
*.pyc
.ruff_cache/

# Local working notes / session logs
notes/
